import { CapacitorConfig } from '@capacitor/cli';

const config: CapacitorConfig = {
  appId: 'is.jonnxor',
  appName: 'JonnXorIs',
  webDir: '../../../dist/apps/client/jonnxor.is',
  bundledWebRuntime: false,
};

export default config;
